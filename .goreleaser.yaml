version: 2
project_name: babu-cli

before:
  hooks:
    - go mod tidy

builds:
  - id: babu-cli
    main: ./cmd/bambu-cli
    binary: babu-cli
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md

checksum:
  name_template: "checksums.txt"

brews:
  - name: babu-cli
    homepage: https://github.com/Dev-devadath/babu-cli
    description: CLI for controlling BambuLab printers directly over MQTT/FTPS/camera.
    repository:
      owner: Dev-devadath
      name: homebrew-tap
      branch: main
      token: "{{ .Env.GORELEASER_TOKEN }}"
    directory: Formula
    test: |
      system "#{bin}/babu-cli", "--help"

release:
  github:
    owner: Dev-devadath
    name: babu-cli
